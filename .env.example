# Django settings (copy to .env and fill in)
DJANGO_SECRET_KEY=your-secret-key-here

# Database (MySQL — leave empty to use SQLite for development)
DB_NAME=LittleLemon
DB_USER=root
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=3306
